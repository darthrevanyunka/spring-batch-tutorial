spring:
  application:
    name: spring-batch-tutorial
  
  # Database Configuration
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  
  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.H2Dialect
  
  # H2 Console
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # Batch Configuration
  batch:
    job:
      enabled: false  # Disable auto-start of jobs
    jdbc:
      initialize-schema: always
    job-repository:
      table-prefix: BATCH_
  
  # Spring Cloud Task Configuration
  cloud:
    task:
      execution:
        events:
          enabled: true
        cleanup:
          on-task-end: true
    dataflow:
      task:
        platform:
          kubernetes:
            accounts:
              default:
                namespace: default
                imagePullPolicy: IfNotPresent
                imagePullSecret: ""
                limits:
                  memory: 1024Mi
                  cpu: 1000m
                requests:
                  memory: 512Mi
                  cpu: 500m
  
  # Actuator Configuration
  management:
    endpoints:
      web:
        exposure:
          include: health,info,metrics,batch-jobs,batch-job-executions,task-executions
    endpoint:
      health:
        show-details: always
      batch-jobs:
        enabled: true
      batch-job-executions:
        enabled: true
      task-executions:
        enabled: true

# Logging Configuration
logging:
  level:
    com.example.springbatchtutorial: INFO
    com.example.springbatchtutorial.controller: WARN
    org.springframework.batch: INFO
    org.springframework.cloud.task: INFO
    org.springframework.cloud.dataflow: INFO
    org.hibernate: ERROR
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Server Configuration
server:
  port: 8080

# Batch Configuration
batch:
  csv:
    file:
      path: input/persons.csv

# Task Configuration for SCDF
task:
  execution:
    events:
      enabled: true
    cleanup:
      on-task-end: true 